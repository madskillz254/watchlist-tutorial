from app import app #basically app instance is like a class name 

if __name__=="__main__":#We then check if the script is run directly 
    app.run()  # app.run() method to launch our server.# debug=true to allow us to run on debug mode so that we can easily track errors in our application.

    
  